# Verificación del MVP SoyCurioso

Comandos esperados antes de publicar:

```bash
npm test
npm run build
npm run check:links
```

También se debe servir `dist/` o usar `npm run preview` para comprobar HTTP local y después verificar GitHub Pages en `https://valenfer.github.io/soycurioso/`.
